# Find the path to the Python executable file
which python

# Check the `/usr/bin` directory for other executables that contain
# the word 'python'
ls /usr/bin/ | grep python

# Check the version of the python executable
python --version

# Run Python in Shell mode
python

# Check the version if the python3 executable
python3 --version

# Run Python3 in Shell mode
python3